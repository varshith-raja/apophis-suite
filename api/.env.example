DATABASE_URL="postgresql://apophis:apophis@localhost:5432/apophis?schema=public"
JWT_SECRET="change-me-in-production"
PORT=4000

# Email (optional). Leave blank for zero-config preview mode (logs instead of sending).
# Set these to go live with real reminder emails:
# SMTP_HOST="smtp.resend.com"
# SMTP_PORT=587
# SMTP_USER="resend"
# SMTP_PASS="your-api-key"
# MAIL_FROM="billing@apophis.in"
