services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: apophis
      POSTGRES_PASSWORD: apophis
      POSTGRES_DB: apophis
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
